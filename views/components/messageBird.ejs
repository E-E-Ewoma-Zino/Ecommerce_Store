


<section>
    <div id="messageBird">
        <% try{ %>
            <% if(message){ %>
                <!-- <div class="message-close">
            <i class="fa fa-arrow-down"></i>
        </div> -->
                <div class="alert alert-<%= message.alert %> alert-dismissible">
                    <button type="button" class="close" data-dismiss="alert">&times;</button>
                   <%= message.message %> 
                </div>
                <% }else{ %>
                    No Message
                    <%} %>
                        <% }catch(err){ %>
                            Error from try
                            <% } %>
    </div>
</section>